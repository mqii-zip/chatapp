'use client';
import { FormEvent, useState } from 'react';
import { useRouter } from 'next/navigation';
import { api } from '../../lib/api';
export default function Register(){const [username,setUsername]=useState('');const [name,setName]=useState('');const [password,setPassword]=useState('');const [err,setErr]=useState('');const r=useRouter();async function submit(e:FormEvent){e.preventDefault();setErr('');try{const d=await api('/auth/register',{method:'POST',body:JSON.stringify({username,displayName:name,password})});localStorage.setItem('token',d.accessToken);localStorage.setItem('user',JSON.stringify(d.user));r.push('/chat')}catch(e:any){setErr('Não foi possível criar a conta. Username pode já estar em uso.')}}return <main className="shell"><form className="card" onSubmit={submit}><h1>Criar conta</h1><div className="field"><label>Username</label><input value={username} onChange={e=>setUsername(e.target.value)} placeholder="luiz_123" required/></div><div className="field"><label>Nome</label><input value={name} onChange={e=>setName(e.target.value)} placeholder="Luiz" required/></div><div className="field"><label>Senha</label><input value={password} onChange={e=>setPassword(e.target.value)} type="password" minLength={6} required/></div>{err&&<p>{err}</p>}<button className="btn">Criar conta</button><p className="muted">Já possui conta? <a href="/login">Entrar</a></p></form></main>}

'use client';
import { FormEvent, useState } from 'react';
import { useRouter } from 'next/navigation';
import { api } from '../../lib/api';
export default function Login(){const [username,setUsername]=useState('');const [password,setPassword]=useState('');const [err,setErr]=useState('');const r=useRouter();async function submit(e:FormEvent){e.preventDefault();setErr('');try{const d=await api('/auth/login',{method:'POST',body:JSON.stringify({username,password})});localStorage.setItem('token',d.accessToken);localStorage.setItem('user',JSON.stringify(d.user));r.push('/chat')}catch{setErr('Usuário ou senha inválidos')}}return <main className="shell"><form className="card" onSubmit={submit}><h1>Entrar</h1><p className="muted">Entre usando apenas seu username.</p><div className="field"><label>Username</label><input value={username} onChange={e=>setUsername(e.target.value)} placeholder="luiz" required/></div><div className="field"><label>Senha</label><input value={password} onChange={e=>setPassword(e.target.value)} type="password" required/></div>{err&&<p>{err}</p>}<button className="btn">Entrar</button><p className="muted">Ainda não tem conta? <a href="/register">Criar conta</a></p></form></main>}
